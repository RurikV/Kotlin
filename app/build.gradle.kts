plugins {
    id("top.ajasta.jvm-convention")
}

group = "top.ajasta"
version = "0.0.1"

// Minimal placeholder project to satisfy external invocations like `:app:test`.
// No sources are required; the `test` task will run with zero tests and succeed.
